# Nexus — Python dependencies
# Install with: pip install -r requirements.txt

# Core
pydantic>=2.0.0
python-dotenv>=1.0.0

# MCP protocol
mcp>=1.0.0

# Async SQLite (local CRM backend)
aiosqlite>=0.20.0

# Google ADK + Gemini (agents — added in Week 2, Days 8–9)
google-adk[extensions]>=1.29.0  # Apache 2.0; [extensions] pulls in litellm for multi-provider support

# Research MCP server (added in Week 1 Days 5-7)
httpx>=0.27.0           # Jina Reader scraping
ddgs>=0.1               # Keyless web search (drop-in SerpAPI replacement; formerly duckduckgo-search)

# Integration webhook receiver (added in Week 4)
# fastapi>=0.115.0
# uvicorn>=0.30.0
